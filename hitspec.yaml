# hitspec configuration
defaultEnvironment: dev
timeout: 30000
followRedirects: true
validateSSL: true

environments:
  dev:
    baseUrl: http://localhost:8080/api/v1
    token: "${TINYVAULT_API_TOKEN}"
  prod:
    baseUrl: https://tinyvault.dev/api/v1
    token: "${TINYVAULT_API_TOKEN}"
